name: Apply customer menu UX final v2

on:
  push:
    branches:
      - codex/customer-menu-ux-final-v2
    paths:
      - scripts/apply-customer-menu-ux-final-v2.mjs
      - .github/workflows/apply-customer-menu-ux-final-v2.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/customer-menu-ux-final-v2
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply targeted patch
        run: |
          node --check scripts/apply-customer-menu-ux-final-v2.mjs
          node scripts/apply-customer-menu-ux-final-v2.mjs
          node --check src/utils/foodOrderingActionState.js
          git diff --check
      - name: Commit generated changes
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(customer): complete menu ordering UX hardening"
          git push origin HEAD:codex/customer-menu-ux-final-v2
