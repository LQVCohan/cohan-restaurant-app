name: Apply current food detail smoke mocks

on:
  push:
    branches:
      - codex/customer-menu-ux-final-v2
    paths:
      - scripts/apply-smoke-food-detail-mocks.mjs
      - .github/workflows/apply-smoke-food-detail-mocks.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/customer-menu-ux-final-v2
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply targeted smoke mock patch
        run: |
          node --check scripts/apply-smoke-food-detail-mocks.mjs
          node scripts/apply-smoke-food-detail-mocks.mjs
          node --check tests/e2e/smoke/graphqlMocks.js
          git diff --check
      - name: Commit generated changes
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "test(smoke): align food detail GraphQL operations"
          git push origin HEAD:codex/customer-menu-ux-final-v2
