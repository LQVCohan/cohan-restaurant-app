type AuditLog {
  id: ID!
  restaurantId: ID
  entity: String
  entityId: ID
  action: String!
  byUserId: ID
  diff: JSON
  actorId: ID
  actorName: String
  actorRole: String
  module: String
  targetType: String
  targetId: ID
  targetName: String
  metadata: JSON
  createdAt: DateTime
  updatedAt: DateTime
}

input AuditLogFilterInput {
  restaurantId: ID
  entity: String
  entityId: ID
  action: String
  byUserId: ID
}

type AuditLogConnection {
  items: [AuditLog!]!
  total: Int!
}

extend type Query {
  auditLogs(
    filter: AuditLogFilterInput
    limit: Int = 50
    offset: Int = 0
  ): AuditLogConnection!
}