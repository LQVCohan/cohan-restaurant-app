import React from "react"; import { fireEvent, render, screen, waitFor, within } from "@testing-library/react"; import { beforeEach, describe, expect, it, vi } from "vitest"; import BackupManagement from "./BackupManagement"; import { AuthContext } from "../../../context/AuthContext"; const useQueryMock = vi.fn(); const useMutationMock = vi.fn(); const useLazyQueryMock = vi.fn(); const previewExport = vi.fn(); const exportBackup = vi.fn(); const previewImport = vi.fn(); const importBackup = vi.fn(); const createBackupRun = vi.fn(); const updateBackupRun = vi.fn(); const scopeState = vi.hoisted(() => ({ current: null, })); const permissionState = vi.hoisted(() => ({ allowed: new Set(["backup.read", "backup.write", "backup.export", "backup.import"]), })); const queryState = { readinessRestaurantId: "", runRestaurantId: "", readinessError: null, runsError: null, }; vi.mock("@apollo/client", () => ({ gql: (strings) => strings, useQuery: (...args) => useQueryMock(...args), useMutation: (...args) => useMutationMock(...args), useLazyQuery: (...args) => useLazyQueryMock(...args), })); vi.mock("../../../hooks/useManagerRestaurantSelection", () => ({ default: () => scopeState.current, })); vi.mock("../../../utils/frontendPermissionAccess", () => ({ hasPermission: (_user, code) => permissionState.allowed.has(code), })); vi.mock("../shared/ManagementPageHeader", () => ({ default: ({ title, subtitle, customControls, stats = [] }) => ( <header> <h1>{title}</h1> <p>{subtitle}</p> {stats.map((item) => <span key={item.label}>{item.label}: {item.value}</span>)} {customControls} </header> ), })); const restaurants = [ { id: "r1", name: "Nhà hàng 1" }, { id: "r2", name: "Nhà hàng 2" }, ]; const makeScope = (restaurantId = "r2") => ({ restaurantOptions: restaurants, selectedRestaurantId: restaurantId, selectedRestaurant: restaurants.find((restaurant) => restaurant.id === restaurantId), restaurantsLoading: false, }); const readinessFor = (restaurantId) => ({ restaurantId, ready: false, risks: [ { key: "reportsChecked", label: "Báo cáo cuối ngày chưa kiểm tra", severity: "warning", resolved: false, description: "Cần hoàn tất" }, ], checklist: { reportsChecked: false, transactionsReconciled: false, settingsReviewed: false, exportPrepared: false, safeCopyStored: false, operatorRecorded: false, }, scope: { ordersAndPayments: false, tablesAndFloorPlan: true, menuAndPricing: true, inventory: true, staffAndPermissions: false, schedules: true, customersAndPromotions: true, reportsAndReconciliation: false, }, lastRun: null, }); const renderPage = () => render( <AuthContext.Provider value={{ user: { id: "manager-1", roleName: "manager" }, restaurants }}> <BackupManagement /> </AuthContext.Provider>, ); const getBackupFileInput = () => screen.getByLabelText(/File sao lưu/, { selector: 'input[type="file"]' }); const latestQueryOptions = (operationName) => [...useQueryMock.mock.calls] .reverse() .find(([query]) => String(query).includes(`query ${operationName}`))?.[1]; beforeEach(() => { vi.clearAllMocks(); scopeState.current = makeScope("r2"); permissionState.allowed = new Set(["backup.read", "backup.write", "backup.export", "backup.import"]); queryState.readinessRestaurantId = ""; queryState.runRestaurantId = ""; queryState.readinessError = null; queryState.runsError = null; useQueryMock.mockImplementation((query, options = {}) => { const text = String(query); const requestedRestaurantId = options.variables?.restaurantId || ""; if (text.includes("query BackupRuns")) { const restaurantId = queryState.runRestaurantId || requestedRestaurantId; return { data: { backupRuns: [] }, loading: false, error: queryState.runsError, refetch: vi.fn(), variables: { restaurantId }, }; } const restaurantId = queryState.readinessRestaurantId || requestedRestaurantId; return { data: { backupReadiness: readinessFor(restaurantId) }, loading: false, error: queryState.readinessError, refetch: vi.fn(), }; }); previewExport.mockResolvedValue({ data: { restaurantConfigBackupPreview: { restaurantId: "r2", fileName: "backup.json", schemaVersion: 1, createdAt: "2026-06-02T00:00:00.000Z", warnings: ["preview warning"], counts: [{ key: "systemSettings", label: "Cài đặt vận hành", count: 1, enabled: true }], }, }, }); exportBackup.mockResolvedValue({ data: { exportRestaurantConfigBackup: { fileName: "backup.json", mimeType: "application/json", encoding: "base64", contentBase64: window.btoa('{"kind":"cohan.restaurant_config_snapshot"}'), checksum: "sha256:abc", sizeBytes: 42, createdAt: "2026-06-02T00:00:00.000Z", }, }, }); previewImport.mockResolvedValue({ data: { previewRestaurantConfigImport: { valid: true, schemaVersion: 1, sourceRestaurantName: "Nguồn", targetRestaurantId: "r2", mode: "clone", warnings: ["preview ok"], errors: [], changes: [{ section: "systemSettings", action: "create", label: "Cài đặt vận hành", count: 1 }], conflictSummary: [], conflicts: [], }, }, }); importBackup.mockResolvedValue({ data: { importRestaurantConfigBackup: { success: true, dryRun: false, targetRestaurantId: "r2", mode: "clone", warnings: ["Skipped recipe ingredient line because ingredient was not imported or could not be remapped."], errors: [], changes: [], conflicts: [], appliedResolutions: [], backupRun: { id: "br1", status: "checklist_completed", note: "Imported", createdAt: "2026-06-02T00:00:00.000Z", completedAt: "2026-06-02T00:00:00.000Z", }, }, }, }); createBackupRun.mockResolvedValue({ data: { createBackupRun: { id: "br-new", restaurantId: "r2", status: "planned", checklist: {}, scope: {}, note: "" } } }); updateBackupRun.mockResolvedValue({ data: { updateBackupRun: { id: "br1", restaurantId: "r2", status: "checklist_completed", checklist: {}, scope: {}, note: "" } } }); useLazyQueryMock.mockImplementation(() => [previewExport, { loading: false }]); useMutationMock.mockImplementation((mutation) => { const text = String(mutation); if (text.includes("createBackupRun")) return [createBackupRun, { loading: false }]; if (text.includes("updateBackupRun")) return [updateBackupRun, { loading: false }]; if (text.includes("exportRestaurantConfigBackup")) return [exportBackup, { loading: false }]; if (text.includes("previewRestaurantConfigImport")) return [previewImport, { loading: false }]; if (text.includes("importRestaurantConfigBackup")) return [importBackup, { loading: false }]; return [vi.fn(), { loading: false }]; }); global.URL.createObjectURL = vi.fn(() => "blob:backup"); global.URL.revokeObjectURL = vi.fn(); vi.spyOn(HTMLAnchorElement.prototype, "click").mockImplementation(() => {}); }); describe("BackupManagement scope and snapshot UI", () => { it("queries the canonical manager restaurant instead of the first auth restaurant", async () => { renderPage(); await waitFor(() => expect(latestQueryOptions("BackupReadiness")?.variables).toEqual({ restaurantId: "r2" })); expect(latestQueryOptions("BackupRuns")?.variables).toEqual({ restaurantId: "r2", limit: 5, offset: 0 }); expect(screen.getAllByText("Nhà hàng 2").length).toBeGreaterThan(0); }); it("switches queries and restore target when the manager scope changes", async () => { const view = renderPage(); await waitFor(() => expect(latestQueryOptions("BackupReadiness")?.variables.restaurantId).toBe("r2")); scopeState.current = makeScope("r1"); view.rerender( <AuthContext.Provider value={{ user: { id: "manager-1", roleName: "manager" }, restaurants }}> <BackupManagement /> </AuthContext.Provider>, ); await waitFor(() => expect(latestQueryOptions("BackupReadiness")?.variables.restaurantId).toBe("r1")); await waitFor(() => expect(screen.getByLabelText("Khôi phục vào nhà hàng")).toHaveValue("r1")); }); it("ignores readiness returned for another restaurant", () => { queryState.readinessRestaurantId = "r1"; renderPage(); expect(screen.getByText(/Dữ liệu trả về không thuộc chi nhánh/)).toBeInTheDocument(); expect(screen.getByText("An toàn: Cần rà soát")).toBeInTheDocument(); }); it("renders compact export/import workflows and the runtime-data warning", () => { renderPage(); expect(screen.getByRole("heading", { name: "Tải file cấu hình" })).toBeInTheDocument(); expect(screen.getByRole("heading", { name: "Khôi phục từ file" })).toBeInTheDocument(); expect(screen.getAllByText(/Đơn hàng, giao dịch và dữ liệu vận hành không nằm trong snapshot/i).length).toBeGreaterThan(0); }); it("creates a manual backup run with the backend-supported scope", async () => { renderPage(); fireEvent.click(screen.getByLabelText("Kiểm tra báo cáo cuối ngày")); fireEvent.click(screen.getByRole("button", { name: /Tạo lần chuẩn bị/ })); await waitFor(() => expect(createBackupRun).toHaveBeenCalled()); const input = createBackupRun.mock.calls[0][0].variables.input; expect(input.restaurantId).toBe("r2"); expect(input.checklist.reportsChecked).toBe(true); expect(input.scope.ordersAndPayments).toBe(false); expect(input.scope.staffAndPermissions).toBe(false); expect(input.scope.reportsAndReconciliation).toBe(false); }); it("requires at least one export section", () => { renderPage(); const exportPanel = screen.getByRole("region", { name: "Tải file sao lưu" }); fireEvent.click(within(exportPanel).getByRole("button", { name: "Bỏ chọn tất cả" })); expect(within(exportPanel).getByText("Chọn ít nhất một hạng mục để tạo file.")).toBeInTheDocument(); expect(within(exportPanel).getByRole("button", { name: /Xem nội dung/ })).toBeDisabled(); expect(within(exportPanel).getByRole("button", { name: /Tải file sao lưu/ })).toBeDisabled(); }); it("selects sections and previews the real export payload", async () => { renderPage(); fireEvent.click(screen.getAllByLabelText("Cài đặt vận hành")[0]); fireEvent.click(screen.getByRole("button", { name: /Xem nội dung/ })); await waitFor(() => expect(previewExport).toHaveBeenCalled()); expect(previewExport.mock.calls[0][0].variables.input).toMatchObject({ restaurantId: "r2", sections: { systemSettings: false }, }); }); it("downloads the exported base64 file", async () => { const appendSpy = vi.spyOn(document.body, "appendChild"); renderPage(); fireEvent.click(screen.getByRole("button", { name: /Tải file sao lưu/ })); await waitFor(() => expect(exportBackup).toHaveBeenCalled()); expect(URL.createObjectURL).toHaveBeenCalled(); expect(appendSpy).toHaveBeenCalled(); expect(await screen.findByText(/Mã kiểm tra: sha256:abc/)).toBeInTheDocument(); }); it("rejects a non-JSON file before calling the backend", async () => { renderPage(); fireEvent.change(getBackupFileInput(), { target: { files: [new File(["x"], "backup.txt", { type: "text/plain" })] }, }); expect(await screen.findByText("Chỉ chấp nhận file sao lưu định dạng JSON.")).toBeInTheDocument(); expect(previewImport).not.toHaveBeenCalled(); }); it("previews import and enables apply only after explicit confirmation", async () => { renderPage(); const file = new File(["{}"], "backup.json", { type: "application/json" }); fireEvent.change(getBackupFileInput(), { target: { files: [file] } }); await waitFor(() => expect(screen.getByText("backup.json")).toBeInTheDocument()); fireEvent.click(screen.getByRole("button", { name: /Xem trước khôi phục/ })); await waitFor(() => expect(previewImport).toHaveBeenCalled()); const importButton = screen.getByRole("button", { name: /Áp dụng khôi phục/ }); expect(importButton).toBeDisabled(); fireEvent.click(screen.getByLabelText(/Tôi đã xem đúng nhà hàng đích/)); expect(importButton).not.toBeDisabled(); fireEvent.click(importButton); await waitFor(() => expect(importBackup).toHaveBeenCalled()); expect(await screen.findByText(/Một vài dòng công thức/)).toBeInTheDocument(); }); it("invalidates preview and confirmation when restore inputs change", async () => { renderPage(); fireEvent.change(getBackupFileInput(), { target: { files: [new File(["{}"], "backup.json", { type: "application/json" })] }, }); await waitFor(() => expect(screen.getByRole("button", { name: /Xem trước khôi phục/ })).not.toBeDisabled()); fireEvent.click(screen.getByRole("button", { name: /Xem trước khôi phục/ })); await waitFor(() => expect(screen.getByLabelText(/Tôi đã xem đúng nhà hàng đích/)).toBeInTheDocument()); fireEvent.click(screen.getByLabelText(/Tôi đã xem đúng nhà hàng đích/)); expect(screen.getByRole("button", { name: /Áp dụng khôi phục/ })).not.toBeDisabled(); fireEvent.change(screen.getByLabelText("Khôi phục vào nhà hàng"), { target: { value: "r1" } }); expect(screen.queryByLabelText(/Tôi đã xem đúng nhà hàng đích/)).not.toBeInTheDocument(); expect(screen.getByRole("button", { name: /Áp dụng khôi phục/ })).toBeDisabled(); }); it("shows backend preview errors inline", async () => { previewExport.mockRejectedValueOnce(new Error("resolver missing")); renderPage(); fireEvent.click(screen.getByRole("button", { name: /Xem nội dung/ })); expect(await screen.findByText(/resolver missing/)).toBeInTheDocument(); }); }); const conflictPreviewPayload = (overrides = {}) => ({ data: { previewRestaurantConfigImport: { valid: true, schemaVersion: 1, sourceRestaurantName: "Nguồn", targetRestaurantId: "r2", mode: "clone", warnings: [], errors: [], changes: [], conflictSummary: [{ key: "section:menuCatalog", label: "section menuCatalog", count: 1, enabled: true }], conflicts: [ { id: "menuCatalog:MenuItem:PHO", section: "menuCatalog", entityType: "MenuItem", entityKey: "PHO", label: "Phở bò", severity: "warning", reason: "MenuItem with the same key already exists in target restaurant.", sourceLegacyId: "old-item", targetId: "target-item", defaultResolution: "keep_target", allowedResolutions: ["use_source", "keep_target", "merge", "rename_source", "skip"], warnings: [], fieldDiffs: [{ field: "basePrice", sourceValuePreview: "50000", targetValuePreview: "55000", severity: "warning" }], ...overrides.conflict, }, ], ...overrides.preview, }, }, }); const previewImportWithFile = async () => { fireEvent.change(getBackupFileInput(), { target: { files: [new File(["{}"], "backup.json", { type: "application/json" })] }, }); await waitFor(() => expect(screen.getByRole("button", { name: /Xem trước khôi phục/ })).not.toBeDisabled()); fireEvent.click(screen.getByRole("button", { name: /Xem trước khôi phục/ })); await waitFor(() => expect(previewImport).toHaveBeenCalled()); }; describe("BackupManagement conflict resolver UI", () => { it("renders conflict summary and field diffs", async () => { previewImport.mockResolvedValueOnce(conflictPreviewPayload()); renderPage(); await previewImportWithFile(); expect(await screen.findByRole("heading", { name: "Chọn cách xử lý" })).toBeInTheDocument(); expect(screen.getByText("Tổng mục")).toBeInTheDocument(); fireEvent.click(screen.getByText("Xem khác biệt")); expect(screen.getByText(/Giá bán: file=50000/)).toBeInTheDocument(); }); it("sends changed conflict resolutions in the import payload", async () => { previewImport.mockResolvedValueOnce(conflictPreviewPayload()); renderPage(); await previewImportWithFile(); fireEvent.change(screen.getByLabelText("Cách xử lý PHO"), { target: { value: "use_source" } }); fireEvent.click(screen.getByLabelText(/Tôi đã xem đúng nhà hàng đích/)); fireEvent.click(screen.getByRole("button", { name: /Áp dụng khôi phục/ })); await waitFor(() => expect(importBackup).toHaveBeenCalled()); expect(importBackup.mock.calls[0][0].variables.input.conflictResolutions).toEqual( expect.arrayContaining([expect.objectContaining({ conflictId: "menuCatalog:MenuItem:PHO", resolution: "use_source" })]), ); }); it("requires a name when rename_source is selected", async () => { previewImport.mockResolvedValueOnce(conflictPreviewPayload()); renderPage(); await previewImportWithFile(); fireEvent.change(screen.getByLabelText("Cách xử lý PHO"), { target: { value: "rename_source" } }); expect(screen.queryByLabelText(/Tôi đã xem đúng nhà hàng đích/)).not.toBeInTheDocument(); fireEvent.change(screen.getByLabelText("Tên mới PHO"), { target: { value: "PHO-NEW" } }); expect(screen.getByLabelText(/Tôi đã xem đúng nhà hàng đích/)).toBeInTheDocument(); }); it("bulk keep target updates supported conflicts", async () => { previewImport.mockResolvedValueOnce(conflictPreviewPayload()); renderPage(); await previewImportWithFile(); fireEvent.change(screen.getByLabelText("Cách xử lý PHO"), { target: { value: "use_source" } }); fireEvent.click(screen.getByRole("button", { name: "Giữ bản hiện tại" })); expect(screen.getByLabelText("Cách xử lý PHO")).toHaveValue("keep_target"); }); it("allows a blocking conflict only after a non-skip resolution is selected", async () => { previewImport.mockResolvedValueOnce(conflictPreviewPayload({ conflict: { severity: "blocking", defaultResolution: "skip" }, preview: { valid: false }, })); renderPage(); await previewImportWithFile(); expect(screen.queryByLabelText(/Tôi đã xem đúng nhà hàng đích/)).not.toBeInTheDocument(); fireEvent.change(screen.getByLabelText("Cách xử lý PHO"), { target: { value: "use_source" } }); expect(screen.getByLabelText(/Tôi đã xem đúng nhà hàng đích/)).toBeInTheDocument(); }); });
