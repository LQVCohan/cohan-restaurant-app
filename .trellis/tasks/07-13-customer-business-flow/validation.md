# Validation

Final validation is executed by the repository pull-request CI after the customer business-flow implementation and regression tests are aligned.

The guest duplicate lookup mock follows the production Mongoose query chain, and its ObjectId mock is constructible like the production driver type.
