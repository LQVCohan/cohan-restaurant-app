{
  "id": "07-10-manager-menu-list-visibility",
  "title": "Hiển thị danh sách thực đơn trong modal",
  "status": "implemented_unverified",
  "type": "frontend-usability",
  "scope": [
    "src/components/Dashboard_Manager/Menu/components/StatsSection/CompactMenuStrip.jsx",
    "src/components/Dashboard_Manager/Menu/components/StatsSection/CompactMenuStrip.test.jsx"
  ],
  "validation": {
    "planned": [
      "npm run check:conflicts",
      "vitest run src/components/Dashboard_Manager/Menu/components/StatsSection/CompactMenuStrip.test.jsx",
      "npm run build",
      "browser smoke /manager#menu on desktop and mobile"
    ],
    "completed": [
      "traced Menu schema/resolver through menus(restaurantId), useMenuManagement, MenuManagement and CompactMenuStrip",
      "confirmed CompactMenuStrip is the only caller-backed menu list and already receives all permission-scoped callbacks",
      "moved the four-slot menu grid into the shared Modal component without changing GraphQL or backend contracts",
      "kept create, edit, toggle, copy, audit, delete and inventory callbacks on the existing parent flow",
      "updated focused component tests for opening and operating the menu list modal"
    ],
    "notRun": [
      "npm run check:conflicts is unavailable in the GitHub connector environment",
      "targeted Vitest is unavailable in the GitHub connector environment",
      "npm run build is unavailable in the GitHub connector environment",
      "browser visual review is unavailable in the GitHub connector environment"
    ]
  }
}
