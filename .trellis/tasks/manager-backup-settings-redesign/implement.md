# Implementation plan

1. Read the latest schema, resolver permissions, restaurant scope, audit flow, Apollo operations, UI callers and focused tests.
2. Align the Settings fallback values with backend defaults and replace mixed English wording without changing GraphQL fields.
3. Rebuild the two existing SCSS layers around the manager sage visual system; reuse current markup and controls.
4. Give backup actions clear primary, secondary and danger hierarchy while keeping export/import behavior unchanged.
5. Update only the Settings component test labels and add one fallback-contract check.
6. Run conflict check, the two component tests and build; review the diff for permission or contract drift.
