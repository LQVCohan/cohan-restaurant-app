# Implementation plan

1. Update the table model, GraphQL type/input, and Apollo fragment for the existing detail fields.
2. Correct split result construction in the shared resolver and add focused resolver tests.
3. Pass the latest table snapshot into the open detail modal and cover it in the manager component test.
4. Add a compact class for the 3D/AR action and adjust only the existing table-card SCSS.
5. Review the diff and run the narrow validation commands from the task PRD.
