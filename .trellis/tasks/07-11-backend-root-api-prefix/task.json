{
  "id": "07-11-backend-root-api-prefix",
  "title": "Sửa proxy route tạo model bàn 3D",
  "status": "in_progress",
  "type": "frontend-bugfix",
  "scope": [
    "src/lib/apiBaseUrl.ts",
    "src/lib/apiBaseUrl.test.js",
    "src/lib/apiBaseUrl.test.ts",
    "vite.config.js"
  ],
  "rootCause": "upload.route.js is wrapped with fastify-plugin, so the prefix option passed by createServer is not applied to its routes. The real endpoints remain at backend-root paths such as /table-3d-ai/generate. The mobile/ngrok Vite server did not proxy /table-3d-ai or /table-3d-assets, while the previous attempted fix incorrectly added /api and therefore reached Fastify at a non-existent route.",
  "validation": {
    "planned": [
      "npx vitest run src/lib/apiBaseUrl.test.js src/lib/apiBaseUrl.test.ts vite.config.test.js",
      "npx vitest run src/components/Dashboard_Manager/Table/CustomTableModelBuilderModal.test.jsx",
      "npm run build",
      "manual mobile retry through ngrok"
    ],
    "completed": [
      "read AGENTS.md, Trellis workflow, ponytail, cross-layer guide and relevant UI skills",
      "traced CustomTableModelBuilderModal -> toBackendRootUrl -> Vite/ngrok proxy -> Fastify uploadRoutes",
      "confirmed uploadRoutes is wrapped with fastify-plugin",
      "confirmed backend route tests register the plugin with prefix /api but successfully call root paths such as /upload/sign",
      "identified missing Vite proxies for /table-3d-ai and /table-3d-assets as the original 404 cause"
    ],
    "notRun": []
  }
}